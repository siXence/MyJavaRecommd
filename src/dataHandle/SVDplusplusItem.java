/**
 * Based on distance to cluster items
 * Item vector is multi-dimension
 * Using the thought of SVD++ to build rating matrix 
 * The SVD++ is to extend the itemVector, in other words, there is another userVector to represent the implicit information
 */
package dataHandle;

/**
 * @author xv
 *
 */
public class SVDplusplusItem extends ClusterSVDDist{
	
}
